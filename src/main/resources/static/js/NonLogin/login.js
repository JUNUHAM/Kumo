/* ==========================================
   KUMO 로그인 페이지 Logic
   ========================================== */

function alertSns() {
    // HTML에서 넘겨준 메시지 변수(loginMessages)를 사용
    alert(loginMessages.sns_alert);
}