spring.application.name=Kumo

# url
server.port=8080
server.servlet.context-path=/

# log
logging.level.root=info
logging.level.net.datasa.community=debug
logging.level.org.springframework.security=DEBUG

# MySQL
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# 데이터베이스 이름(kumo_db 부분)은 실제 만드신 스키마 이름으로 바꿔야 합니다.
spring.datasource.url=jdbc:mysql://localhost:3306/kumo_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
spring.datasource.username=root
spring.datasource.password=123

# JPA
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.format_sql=true
logging.level.org.hibernate.type.descriptor.sql=trace

# API Key
kumo.google.maps.key=${GOOGLE_MAPS_KEY}

# ==========================
# [파일 업로드 설정]
# ==========================
# 1. 파일 크기 제한
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=100MB

# 2. (중요) 실제 저장할 경로 변수 선언 (이름은 내 맘대로 지음)
# 주의: 끝에 '/'를 꼭 붙여주세요!
# 기존 경로는 지우고 아래 걸로 교체!
# [맥북 전용 설정]
# 주의: 끝에 슬래시(/) 필수!
file.upload.dir=/Users/hyunwookim/Desktop/kumo_uploads/

#사용자 정의 속성값 설정
board.pageSize=10
board.linkSize=2
board.uploadPath=C:/upload